[tools]
ruby = "4.0.2"

[tasks.setup]
description = "Install dependencies and create local gateway config"
run = "bin/setup"

[tasks.start]
description = "Run the web gateway in production mode"
run = "bin/start"

[tasks.dev]
description = "Run the web gateway in development mode on port 4567"
run = "RACK_ENV=development bundle exec rackup -o 0.0.0.0 -p 4567"

[tasks.test]
description = "Run the test suite"
run = "bundle exec ruby -Itest test/*_test.rb"
