#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
binary="$script_dir/../tmp/gripi"
if [[ ! -x "$binary" ]]; then
  printf 'Built Go gateway not found at %s; run bin/setup first\n' "$binary" >&2
  exit 1
fi
exec "$binary" password
